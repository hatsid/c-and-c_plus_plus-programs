#include <stdio.h>
#include<string.h>

int main()
{
int a[50];
printf("input the string to check the function gets and puts  workings");
gets(a);
printf("the string you have entered ");
puts(a);
    return 0;
}